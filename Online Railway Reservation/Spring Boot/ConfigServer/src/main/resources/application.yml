spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/chandumalireddy/cloud-config-server
          username: username
          password: pass
          default-label: main
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins: "*"
server:
  port: 8086

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    name: localhost